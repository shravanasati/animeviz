services:
  qdrant:
    image: qdrant/qdrant:v1.18
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage:z
    networks:
      - app-network


networks:
  app-network:
    driver: bridge

volumes:
  qdrant_storage:
